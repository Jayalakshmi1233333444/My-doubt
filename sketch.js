var gameState=0,player,form,playerCount,game;
var database,position;
function setup(){
    //creating database
    database=firebase.database();
    createCanvas(400,400);
   game=new Game();//can't understand
   game.getState();//can't understand
   game.start();//can't understand
}

function draw(){
}


